{% extends 'base.html.twig' %}

{% block title %}EUROKEYS{% endblock %}

{% block body %}



            <div class="row mb-5 mt-5 justify-content-center">
                {%  for post in posts %}
                    <div class="col-11 col-lg-7 card mb-5 text-center shadow">
                        <h2 class="card-title mt-3">{{post.title}}</h2>
                        <time class="mt-3" datetime={{post.createdAt|date('d-m-Y')}}> 📅 Publié le {{post.createdAt|date('d-m-Y')}}  </time>
                        <p class="card-text mt-3">{{post.description|slice(0,20)}}... </p>
                        <a href="#" class="mb-3">Lire...</a>
                        <img class="card-img-top mb-3" src="{{ (post.imageName ? vich_uploader_asset(post) : asset('images/placeholder.jpg'))}}" alt="" />
                    </div>
                {% endfor %}
            </div>
      
{% endblock %}
